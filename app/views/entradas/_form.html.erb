<%= form_with(model: entrada) do |form| %>
  <% if entrada.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(entrada.errors.count, "error") %> prohibited this entrada from being saved:</h2>

      <ul>
        <% entrada.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :funcion_id, style: "display: block" %>
    <%= form.text_field :funcion_id %>
  </div>

  <div>
    <%= form.label :usuario_id, style: "display: block" %>
    <%= form.text_field :usuario_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
